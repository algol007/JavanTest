# How to run this project

1. clone git repository using git clone `git@github.com:algol007/JavanTest.git`
2. enter to folder/directory `cd TestJavan`
3. run `yarn install` / `npm install` from terminal
4. import db_family.sql to your local database
5. run `node server.js`
